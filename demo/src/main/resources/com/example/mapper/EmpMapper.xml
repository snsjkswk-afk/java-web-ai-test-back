<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 命名空间随便写，不影响 -->
<mapper namespace="com.example.mapper.EmpMapper">
    <update id="update">
        update emp
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 直接引用外部 SQL 片段 -->

    <select id='list' resultType="com.example.pojo.Emp">
        select e.*,d.name deptName from emp e left join  dept d on e.dept_id = d.id
        <where>
            <if test="name != null and  name != ''">
                 e.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                 e.gender = #{gender}
            </if>
        <if test="begin != null and end != null ">
             e.entry_date between #{begin} and #{end}
        </if>

        </where>
    </select>

    <select id="getById"  resultMap="getByIdResultMap">
        select e.*,ee.id ee_id,ee.begin ee_begin,ee.end ee_end,ee.company ee_company,ee.job ee_job
            from emp e left join emp_expr ee on e.id = ee.emp_id where e.id = #{id}
    </select>
    <select id="getJobData" resultType="java.util.Map">
        select (case job when 1 then '班主任'
             when 2 then '讲师'
             when 3 then '学工主管'
             when 4 then '教研主管'
             when 5 then '咨询师'
             else '其他' end) job,
             count(*) count
             from emp group by job
    </select>
    <select id="getGenderData" resultType="java.util.Map">
            select if(gender = 1, '男', '女')
            as gender,
             count(*) count
            from emp group by gender
    </select>
    <select id="getStudentDegreeData" resultType="java.util.Map">
        select (case degree when 1 then '本科'
             when 2 then '硕士'
             when 3 then '博士'
             else '其他' end) degree,
             count(*) count
             from student group by degree
    </select>
    <select id="getStudentCountData" resultType="java.util.Map">
        select (case clazz_id when 1 then '一班'
                              when 2 then '二班'
                              when 3 then '三班'
                              when 4 then '四班'
                              when 5 then '五班'
                              else '其他' end) clazz,
                              count(*) count
                              from student group by clazz_id;
    </select>
    <select id="login" resultType="com.example.pojo.Emp">
        select * from emp where username = #{username} and password = #{password}
    </select>


    <resultMap id="getByIdResultMap" type="com.example.pojo.Emp" >
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="name" column="name" />
        <result property="gender" column="gender" />
        <result property="phone" column="phone" />
        <result property="job" column="job" />
        <result property="salary" column="salary" />
        <result property="image" column="image" />
        <result property="entryDate" column="entry_date" />
        <result property="deptId" column="dept_id" />
        <result property="createTime" column="createTime"/>
        <result property="updateTime" column="updateTime"/>

        <collection property="exprList" ofType="com.example.pojo.EmpExpr">
            <id property="id" column="ee_id"/>
            <result property="begin" column="ee_begin"/>
            <result property="end" column="ee_end"/>
            <result property="company" column="ee_company"/>
        </collection>
    </resultMap>


    <insert id="save" parameterType="com.example.pojo.Emp"  useGeneratedKeys="true" keyProperty="id">
        insert into emp(username, name, gender, phone, job, salary, image, entry_date, dept_id, create_time, update_time)
        values (#{username},#{name},#{gender},#{phone},#{job},#{salary},#{image},#{entryDate},#{deptId},#{createTime},#{updateTime})
    </insert>
</mapper>